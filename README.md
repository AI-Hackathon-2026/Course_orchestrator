# Сourse_orchestrator

A course orchestrator container for working with grafs, topics, and databases
with them

### How to launch:

Clone repository, run docker build and run container with env's values

### env's values:

LANGFUSE_SECRET_KEY, LANGFUSE_PUBLIC_KEY
